pipeline {
  agent {
    docker {
      image 'node:8.11.4'
    }
  }
   stages {
    stage ('Build') {
      steps {
        script {
          sh "npm install"
          sh "./node_modules/bower/bin/bower install"
        }
      }
    }
  }
}