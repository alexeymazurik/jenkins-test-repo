#!groovy
node('node') {
  currentBuild.result = "SUCCESS"

  try {
    stage('Checkout'){
      checkout scm
    }

    stage('Build'){
      sh 'node -v'
      sh 'npm i'
      sh './node_modules/bower/bin/bower i'
    }
  }
  catch(err) {
    currentBuild.result = "SUCCESS"
    throw err
  }
}